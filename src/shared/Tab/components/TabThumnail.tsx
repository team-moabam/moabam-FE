import React from 'react';

export interface TabThumnailProps {
  children: React.ReactNode;
}

const TabThumnail = ({ children }: TabThumnailProps) => {
  return children;
};

export default TabThumnail;
